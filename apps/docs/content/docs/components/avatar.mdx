---
title: Avatar
description: An image element with a fallback for representing the user.
---

import { PreviewCard } from '@/components/ui/PreviewCard';
import { AvatarPreview } from '@/components/examples';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

<Tabs items={['preview', 'code']}>
  <Tab value="preview">
    <PreviewCard preview={<AvatarPreview />} />
  </Tab>
  <Tab value="code">
    ```tsx title="avatar-example.tsx"
    import { Avatar, AvatarImage, AvatarFallback } from '@/components/ui/avatar';
    import { Text } from '@/components/ui/text';

    export default function AvatarExample() {
      return (
        <Avatar alt='@blencorp'>
          <AvatarImage source={{ uri: 'https://avatars.githubusercontent.com/u/240379?s=400&v=4' }} />
          <AvatarFallback>
            <Text>BN</Text>
          </AvatarFallback>
        </Avatar>
      );
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={['cli', 'manual']}>
  <Tab value="cli">
    ```bash
    npx @blen/usmds add avatar
    ```
  </Tab>
  <Tab value="manual">
    <Steps>
      <Step>
        **Install the following dependencies:**

        ```bash
        npx expo install @rn-primitives/avatar
        ```
      </Step>
      <Step>
        **Copy and paste the following code into your project.**

        ```json doc-gen:file
{
  "file": "../../packages/registry/src/usa/components/ui/avatar.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"@/components/ui/avatar.tsx\""
  }
}
        ```
      </Step>
      <Step>
        **Update the import paths to match your project setup.**
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

```tsx
import { Avatar, AvatarImage, AvatarFallback } from '@/components/ui/avatar';
import { Text } from '@/components/ui/text';

<Avatar>
  <AvatarImage source={{ uri: 'https://github.com/shadcn.png' }} />
  <AvatarFallback>
    <Text>CN</Text>
  </AvatarFallback>
</Avatar>
```

## Props

### Avatar

Extends `@rn-primitives/avatar` Root props.

| Prop | Type | Description |
|------|------|-------------|
| className | `string` | Additional classes for the avatar container. |

### AvatarImage

Extends `@rn-primitives/avatar` Image props.

| Prop | Type | Description |
|------|------|-------------|
| source | `ImageSourcePropType` | The image source. |
| className | `string` | Additional classes for the image. |

### AvatarFallback

Extends `@rn-primitives/avatar` Fallback props.

| Prop | Type | Description |
|------|------|-------------|
| className | `string` | Additional classes for the fallback. |
