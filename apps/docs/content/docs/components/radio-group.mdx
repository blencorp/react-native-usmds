---
title: Radio Group
description: A set of checkable buttons—known as radio buttons—where no more than one of the buttons can be checked at a time.
---

import { PreviewCard } from '@/components/ui/PreviewCard';
import { RadioGroupPreview } from '@/components/examples';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

<Tabs items={['preview', 'code']}>
  <Tab value="preview">
    <PreviewCard preview={<RadioGroupPreview />} />
  </Tab>
  <Tab value="code">
    ```tsx title="radio-group-example.tsx"
    import { RadioGroup, RadioGroupItem } from '@/components/ui/radiogroup';
    import { Label } from '@/components/ui/label';
    import { useState } from 'react';

    export default function RadioGroupExample() {
      const [value, setValue] = useState('comfortable');

      return (
        <RadioGroup value={value} onValueChange={setValue}>
          <div className="flex items-center space-x-2">
            <RadioGroupItem value="default" id="r1" />
            <Label htmlFor="r1">Default</Label>
          </div>
          <div className="flex items-center space-x-2">
            <RadioGroupItem value="comfortable" id="r2" />
            <Label htmlFor="r2">Comfortable</Label>
          </div>
          <div className="flex items-center space-x-2">
            <RadioGroupItem value="compact" id="r3" />
            <Label htmlFor="r3">Compact</Label>
          </div>
        </RadioGroup>
      );
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={['cli', 'manual']}>
  <Tab value="cli">
    ```bash
    npx @blen/usmds add radiogroup
    ```
  </Tab>
  <Tab value="manual">
    <Steps>
      <Step>
        **Install the following dependencies:**

        ```bash
        npx expo install @rn-primitives/radio-group
        ```
      </Step>
      <Step>
        **Copy and paste the following code into your project.**

        ```json doc-gen:file
{
  "file": "../../packages/registry/src/usa/components/ui/radiogroup.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"@/components/ui/radiogroup.tsx\""
  }
}
        ```
      </Step>
      <Step>
        **Update the import paths to match your project setup.**
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

```tsx
import { RadioGroup, RadioGroupItem } from '@/components/ui/radiogroup';
import { Label } from '@/components/ui/label';
import { useState } from 'react';

const [value, setValue] = useState('option-one');

<RadioGroup value={value} onValueChange={setValue}>
  <div className="flex items-center space-x-2">
    <RadioGroupItem value="option-one" id="option-one" />
    <Label htmlFor="option-one">Option One</Label>
  </div>
  <div className="flex items-center space-x-2">
    <RadioGroupItem value="option-two" id="option-two" />
    <Label htmlFor="option-two">Option Two</Label>
  </div>
</RadioGroup>
```

## Props

### RadioGroup

Extends `@rn-primitives/radio-group` Root props.

| Prop | Type | Description |
|------|------|-------------|
| value | `string` | The controlled value of the radio item to check. |
| onValueChange | `(value: string) => void` | Event handler called when the value changes. |
| disabled | `boolean` | When true, prevents the user from interacting with radio items. |
| className | `string` | Additional classes for styling. |

### RadioGroupItem

Extends `@rn-primitives/radio-group` Item props.

| Prop | Type | Description |
|------|------|-------------|
| value | `string` | **Required.** The value of the radio item. |
| id | `string` | The id of the radio item (for label association). |
| disabled | `boolean` | When true, prevents the user from interacting with the radio item. |
| className | `string` | Additional classes for styling. |

