---
title: Toggle
description: A two-state button that can be either on or off.
---

import { PreviewCard } from '@/components/ui/PreviewCard';
import { TogglePreview } from '@/components/examples';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

<Tabs items={['preview', 'code']}>
  <Tab value="preview">
    <PreviewCard preview={<TogglePreview />} />
  </Tab>
  <Tab value="code">
    ```tsx title="toggle-example.tsx"
    import { Toggle, ToggleIcon } from '@/components/ui/toggle';
    import { Bold } from 'lucide-react-native';
    import { useState } from 'react';

    export default function ToggleExample() {
      const [pressed, setPressed] = useState(false);

      return (
        <Toggle pressed={pressed} onPressedChange={setPressed}>
          <ToggleIcon as={Bold} />
        </Toggle>
      );
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={['cli', 'manual']}>
  <Tab value="cli">
    ```bash
    npx @blen/usmds add toggle
    ```
  </Tab>
  <Tab value="manual">
    <Steps>
      <Step>
        **Install the following dependencies:**

        ```bash
        npx expo install @rn-primitives/toggle lucide-react-native
        ```
      </Step>
      <Step>
        **Copy and paste the following code into your project.**

        ```json doc-gen:file
{
  "file": "../../packages/registry/src/usa/components/ui/toggle.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"@/components/ui/toggle.tsx\""
  }
}
        ```
      </Step>
      <Step>
        **Update the import paths to match your project setup.**
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

```tsx
import { Toggle, ToggleIcon } from '@/components/ui/toggle';
import { Bold } from 'lucide-react-native';
import { useState } from 'react';

const [pressed, setPressed] = useState(false);

<Toggle pressed={pressed} onPressedChange={setPressed}>
  <ToggleIcon as={Bold} />
</Toggle>
```

## Props

### Toggle

Extends `TogglePrimitive.Root` props.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| pressed | `boolean` | `false` | The controlled pressed state of the toggle. |
| onPressedChange | `(pressed: boolean) => void` | - | Callback when pressed state changes. |
| variant | `'default' \| 'outline'` | `'default'` | The visual style variant. |
| size | `'default' \| 'sm' \| 'lg'` | `'default'` | The size of the toggle. |
| disabled | `boolean` | `false` | When true, prevents user interaction. |
| className | `string` | - | Additional classes for styling. |

### ToggleIcon

Extends `Icon` props.

| Prop | Type | Description |
|------|------|-------------|
| as | `LucideIcon` | The icon component to display. |
| className | `string` | Additional classes for styling. |

## Variants

### Default

```tsx
import { Toggle, ToggleIcon } from '@/components/ui/toggle';
import { Bold } from 'lucide-react-native';
import { useState } from 'react';

function DefaultToggle() {
  const [pressed, setPressed] = useState(false);

  return (
    <Toggle pressed={pressed} onPressedChange={setPressed}>
      <ToggleIcon as={Bold} />
    </Toggle>
  );
}
```

### Outline

```tsx
<Toggle variant="outline" pressed={pressed} onPressedChange={setPressed}>
  <ToggleIcon as={Bold} />
</Toggle>
```

## Sizes

### Default

```tsx
<Toggle size="default" pressed={pressed} onPressedChange={setPressed}>
  <ToggleIcon as={Bold} />
</Toggle>
```

### Small

```tsx
<Toggle size="sm" pressed={pressed} onPressedChange={setPressed}>
  <ToggleIcon as={Bold} />
</Toggle>
```

### Large

```tsx
<Toggle size="lg" pressed={pressed} onPressedChange={setPressed}>
  <ToggleIcon as={Bold} />
</Toggle>
```


## Helper Functions

### toggleVariants

Use `toggleVariants` to create custom toggle-styled components:

```tsx
import { toggleVariants } from '@/components/ui/toggle';
import { Pressable } from 'react-native';

<Pressable className={toggleVariants({ variant: 'outline', size: 'lg' })}>
  {/* custom content */}
</Pressable>
```
