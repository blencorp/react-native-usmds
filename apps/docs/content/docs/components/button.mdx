---
title: Button
description: Buttons allow users to trigger actions with a single tap.
---
import {DemoButton} from '@/components/ui/DemoButton';

<DemoButton href="https://usmds-showcase.expo.app/components/button"/>

## Installation

```package-install
npx usmds add button
```

## Usage

```tsx
import { Button } from '@components/Button';
import { Text } from '@components/Text';

<Button>
  <Text>Button</Text>
</Button>
```

## Props

### Button

Extends `Pressable` props.

<table>
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>variant</td>
      <td>`'default' | 'secondary' | 'accent-cool' | 'accent-warm' | 'base' | 'outline' | 'inverse'`</td>
      <td>`'default'`</td>
      <td>The visual style variant of the button.</td>
    </tr>
    <tr>
      <td>size</td>
      <td>`'default' | 'sm' | 'lg' | 'big' | 'icon'`</td>
      <td>`'default'`</td>
      <td>The size variant of the button.</td>
    </tr>
    <tr>
      <td>startIcon</td>
      <td>`ReactNode`</td>
      <td>-</td>
      <td>Icon element to display before the button content.</td>
    </tr>
    <tr>
      <td>endIcon</td>
      <td>`ReactNode`</td>
      <td>-</td>
      <td>Icon element to display after the button content.</td>
    </tr>
    <tr>
      <td>disabled</td>
      <td>`boolean`</td>
      <td>`false`</td>
      <td>When true, disables button interactions.</td>
    </tr>
    <tr>
      <td>className</td>
      <td>`string`</td>
      <td>-</td>
      <td>Additional Tailwind classes to apply.</td>
    </tr>
  </tbody>
</table>

## Variants

### Default (Primary)

```tsx
<Button variant="default">
  <Text>Primary</Text>
</Button>
```

### Secondary

```tsx
<Button variant="secondary">
  <Text>Secondary</Text>
</Button>
```

### Accent Cool

```tsx
<Button variant="accent-cool">
  <Text>Accent Cool</Text>
</Button>
```

### Accent Warm

```tsx
<Button variant="accent-warm">
  <Text>Accent Warm</Text>
</Button>
```

### Base

```tsx
<Button variant="base">
  <Text>Base</Text>
</Button>
```

### Outline

```tsx
<Button variant="outline">
  <Text>Outline</Text>
</Button>
```

### Inverse

```tsx
<Button variant="inverse">
  <Text>Inverse</Text>
</Button>
```

## Sizes

### Small
```tsx
<Button size="sm">
  <Text>Small Button</Text>
</Button>
```

### Default
```tsx
<Button size="default">
  <Text>Default Button</Text>
</Button>
```

### Large
```tsx
<Button size="lg">
  <Text>Large Button</Text>
</Button>
```

### Big
```tsx
<Button size="big">
  <Text>Big Button</Text>
</Button>
```

### Icon
```tsx
<Button size="icon">
  <Mail />
</Button>
```

## With Icons

```tsx
// Start Icon
<Button startIcon={<Mail />}>
  <Text>Start Icon</Text>
</Button>

// End Icon
<Button endIcon={<ArrowRight />}>
  <Text>End Icon</Text>
</Button>

// Both Icons
<Button startIcon={<Mail />} endIcon={<ArrowRight />}>
  <Text>Both Icons</Text>
</Button>
```

## States

Buttons support various states through className props:

### Hover
```tsx
<Button className="bg-primary-hover">
  <Text>Hover State</Text>
</Button>
```

### Active
```tsx
<Button className="bg-primary-active">
  <Text>Active State</Text>
</Button>
```

### Focus
```tsx
<Button className="ring-2 ring-focus-ring ring-offset-2">
  <Text>Focus State</Text>
</Button>
```

### Disabled
```tsx
<Button disabled>
  <Text>Disabled Button</Text>
</Button>
```

## Interactive State

The Button component accepts a function as children to render content based on the press state:

```tsx
<Button>
  {(state) => (
    <Text>
      {state.pressed ? 'Pressing!' : state.hovered ? 'Hovering!' : 'Interactive'}
    </Text>
  )}
</Button>
```

## Accessibility

The Button component:
- Uses `role="button"` for proper semantic structure
- Supports keyboard navigation
- Maintains WCAG color contrast requirements
- Provides appropriate disabled states
- Includes clear, descriptive text labels