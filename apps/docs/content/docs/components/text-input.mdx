---
title: Text Input
description: A foundational text input component for entering and editing text.
---

import { PreviewCard } from '@/components/ui/PreviewCard';
import { TextInputPreview } from '@/components/examples';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

<Tabs items={['preview', 'code']}>
  <Tab value="preview">
    <PreviewCard preview={<TextInputPreview />} />
  </Tab>
  <Tab value="code">
    ```tsx title="text-input-example.tsx"
    import { TextInput } from '@/components/ui/textinput';
    import { useState } from 'react';

    export default function TextInputExample() {
      const [text, setText] = useState('');

      return (
        <TextInput
          placeholder="Enter text..."
          value={text}
          onChangeText={setText}
        />
      );
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={['cli', 'manual']}>
  <Tab value="cli">
    ```bash
    npx usmds add textinput
    ```
  </Tab>
  <Tab value="manual">
    <Steps>
      <Step>
        **Copy and paste the following code into your project.**

        ```json doc-gen:file
{
  "file": "../../packages/registry/src/usa/components/ui/textinput.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"@/components/ui/textinput.tsx\""
  }
}
        ```
      </Step>
      <Step>
        **Update the import paths to match your project setup.**
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

```tsx
import { TextInput } from '@/components/ui/textinput';
import { useState } from 'react';

const [text, setText] = useState('');

<TextInput
  placeholder="Enter text..."
  value={text}
  onChangeText={setText}
/>
```

## Props

### TextInput

Extends React Native's `TextInput` props.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| className | `string` | - | Additional classes for styling. |
| editable | `boolean` | `true` | When false, input is disabled. |
| placeholder | `string` | - | Placeholder text. |
| value | `string` | - | The input value. |
| onChangeText | `(text: string) => void` | - | Callback when text changes. |

All standard React Native TextInput props are supported.

