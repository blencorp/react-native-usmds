---
title: Checkbox Tile
description: A larger checkbox option with label and optional description text.
---

import { PreviewCard } from '@/components/ui/PreviewCard';
import { CheckboxTilePreview } from '@/components/examples';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

<Tabs items={['preview', 'code']}>
  <Tab value="preview">
    <PreviewCard preview={<CheckboxTilePreview />} />
  </Tab>
  <Tab value="code">
    ```tsx title="checkbox-tile-example.tsx"
    import { CheckboxTile } from '@/components/ui/checkbox-tile';
    import { useState } from 'react';

    export default function CheckboxTileExample() {
      const [checked, setChecked] = useState(false);

      return (
        <CheckboxTile
          label="Checkbox tile"
          description="Optional description text"
          checked={checked}
          onCheckedChange={setChecked}
        />
      );
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={['cli', 'manual']}>
  <Tab value="cli">
    ```bash
    npx usmds add checkbox-tile
    ```
  </Tab>
  <Tab value="manual">
    <Steps>
      <Step>
        **Install the following dependencies:**

        ```bash
        npx expo install @rn-primitives/checkbox lucide-react-native
        ```
      </Step>
      <Step>
        **Copy and paste the following code into your project.**

        ```json doc-gen:file
{
  "file": "../../packages/registry/src/usa/components/ui/checkboxtile.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"@/components/ui/checkbox-tile.tsx\""
  }
}
        ```
      </Step>
      <Step>
        **Update the import paths to match your project setup.**
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

```tsx
import { CheckboxTile } from '@/components/ui/checkbox-tile';
import { useState } from 'react';

const [checked, setChecked] = useState(false);

<CheckboxTile
  label="Checkbox tile"
  checked={checked}
  onCheckedChange={setChecked}
/>
```

## Props

### CheckboxTile

Extends `@rn-primitives/checkbox` Root props.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| label | `string` | - | **Required.** The text label for the checkbox tile. |
| description | `string` | - | Optional description text below the label. |
| checked | `boolean` | - | The controlled checked state. |
| onCheckedChange | `(checked: boolean) => void` | - | Callback when checked state changes. |
| disabled | `boolean` | `false` | Whether the checkbox is disabled. |
| className | `string` | - | Additional classes for styling. |


## Helper Functions

### checkboxTileVariants

Use `checkboxTileVariants` to create custom checkbox tile styled components:

```tsx
import { checkboxTileVariants } from '@/components/ui/checkbox-tile';
import { View } from 'react-native';

<View className={checkboxTileVariants({ variant: 'withDescription', state: 'checked' })}>
  {/* content */}
</View>
```
