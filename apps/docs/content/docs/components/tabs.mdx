---
title: Tabs
description: A set of layered sections of content—known as tab panels—that are displayed one at a time.
---

import { PreviewCard } from '@/components/ui/PreviewCard';
import { TabsPreview } from '@/components/examples';
import { Tabs as DocTabs, Tab } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

<DocTabs items={['preview', 'code']}>
  <Tab value="preview">
    <PreviewCard preview={<TabsPreview />} />
  </Tab>
  <Tab value="code">
    ```tsx title="tabs-example.tsx"
    import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
    import { Text } from '@/components/ui/text';
    import { useState } from 'react';

    export default function TabsExample() {
      const [value, setValue] = useState('account');

      return (
        <Tabs value={value} onValueChange={setValue} className="w-full max-w-md">
          <TabsList className="w-full">
            <TabsTrigger value="account" className="flex-1">
              <Text>Account</Text>
            </TabsTrigger>
            <TabsTrigger value="password" className="flex-1">
              <Text>Password</Text>
            </TabsTrigger>
          </TabsList>
          <TabsContent value="account" className="mt-4">
            <Text>Make changes to your account here. Click save when you're done.</Text>
          </TabsContent>
          <TabsContent value="password" className="mt-4">
            <Text>Change your password here. After saving, you'll be logged out.</Text>
          </TabsContent>
        </Tabs>
      );
    }
    ```
  </Tab>
</DocTabs>

## Installation

<DocTabs items={['cli', 'manual']}>
  <Tab value="cli">
    <Steps>
      <Step>
        ```bash
        npx usmds add tabs
        ```
      </Step>
    </Steps>
  </Tab>
  <Tab value="manual">
    <Steps>
      <Step>
        **Install dependencies:**

        ```bash
        npx expo install @rn-primitives/tabs
        ```
      </Step>
      <Step>
        **Copy and paste the following code into your project.**

        ```json doc-gen:file
{
  "file": "../../packages/registry/src/usa/components/ui/tabs.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"@/components/ui/tabs.tsx\""
  }
}
        ```
      </Step>
      <Step>
        **Update import paths to match your project setup.**
      </Step>
    </Steps>
  </Tab>
</DocTabs>

## Usage

```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { Text } from '@/components/ui/text';

<Tabs defaultValue="tab1">
  <TabsList>
    <TabsTrigger value="tab1">
      <Text>Tab 1</Text>
    </TabsTrigger>
    <TabsTrigger value="tab2">
      <Text>Tab 2</Text>
    </TabsTrigger>
  </TabsList>
  <TabsContent value="tab1">
    <Text>Content 1</Text>
  </TabsContent>
  <TabsContent value="tab2">
    <Text>Content 2</Text>
  </TabsContent>
</Tabs>
```

## Props

### Tabs

Extends `@rn-primitives/tabs` Root props.

| Prop | Type | Description |
|------|------|-------------|
| value | `string` | The controlled value of the tab to activate. |
| onValueChange | `(value: string) => void` | Event handler called when the value changes. |
| defaultValue | `string` | The value of the tab that should be active when initially rendered. |
| orientation | `'horizontal' \| 'vertical'` | The orientation of the tabs. Default is `'horizontal'`. |
| disabled | `boolean` | When true, prevents user interaction. |
| className | `string` | Additional classes for styling. |

### TabsList

| Prop | Type | Description |
|------|------|-------------|
| children | `React.ReactNode` | The tab triggers. |
| className | `string` | Additional classes for styling. |

### TabsTrigger

Extends `@rn-primitives/tabs` Trigger props.

| Prop | Type | Description |
|------|------|-------------|
| value | `string` | **Required.** A unique value that associates the trigger with a content. |
| disabled | `boolean` | When true, prevents the user from activating the tab. |
| className | `string` | Additional classes for styling. |

### TabsContent

Extends `@rn-primitives/tabs` Content props.

| Prop | Type | Description |
|------|------|-------------|
| value | `string` | **Required.** A unique value that associates the content with a trigger. |
| forceMount | `boolean` | Force mounting content even when not active. |
| className | `string` | Additional classes for styling. |

