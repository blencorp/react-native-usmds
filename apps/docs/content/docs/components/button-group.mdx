---
title: Button Group
description: Groups related buttons together with consistent spacing.
---

import { PreviewCard } from '@/components/ui/PreviewCard';
import { ButtonGroupPreview } from '@/components/examples';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

<Tabs items={['preview', 'code']}>
  <Tab value="preview">
    <PreviewCard preview={<ButtonGroupPreview />} />
  </Tab>
  <Tab value="code">
    ```tsx title="button-group-example.tsx"
    import { ButtonGroup } from '@/components/ui/button-group';
    import { Button } from '@/components/ui/button';
    import { Text } from '@/components/ui/text';

    export default function ButtonGroupExample() {
      return (
        <ButtonGroup orientation="vertical">
          <Button variant="default">
            <Text>Primary</Text>
          </Button>
          <Button variant="outline">
            <Text>Secondary</Text>
          </Button>
        </ButtonGroup>
      );
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={['cli', 'manual']}>
  <Tab value="cli">
    ```bash
    npx @blen/usmds add button-group
    ```
  </Tab>
  <Tab value="manual">
    <Steps>
      <Step>
        **Copy and paste the following code into your project.**

        ```json doc-gen:file
{
  "file": "../../packages/registry/src/usa/components/ui/buttongroup.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"@/components/ui/button-group.tsx\""
  }
}
        ```
      </Step>
      <Step>
        **Update the import paths to match your project setup.**
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

```tsx
import { ButtonGroup } from '@/components/ui/button-group';
import { Button } from '@/components/ui/button';
import { Text } from '@/components/ui/text';

<ButtonGroup orientation="vertical">
  <Button variant="default">
    <Text>Primary</Text>
  </Button>
  <Button variant="outline">
    <Text>Secondary</Text>
  </Button>
</ButtonGroup>
```

## Props

### ButtonGroup

Extends `View` props.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| orientation | `'horizontal' \| 'vertical'` | `'vertical'` | The layout direction of the button group. |
| gap | `number` | `8` | The space between buttons in pixels. |
| className | `string` | - | Additional classes for styling. |

