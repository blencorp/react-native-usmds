---
title: Collapsible
description: An interactive component which can be expanded/collapsed to show and hide content.
---

import { PreviewCard } from '@/components/ui/PreviewCard';
import { CollapsiblePreview } from '@/components/examples';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

<Tabs items={['preview', 'code']}>
  <Tab value="preview">
    <PreviewCard preview={<CollapsiblePreview />} />
  </Tab>
  <Tab value="code">
    ```tsx title="collapsible-example.tsx"
    import {
      Collapsible,
      CollapsibleTrigger,
      CollapsibleContent,
    } from '@/components/ui/collapsible';
    import { Button } from '@/components/ui/button';
    import { Text } from '@/components/ui/text';
    import { useState } from 'react';

    export default function CollapsibleExample() {
      const [isOpen, setIsOpen] = useState(false);

      return (
        <Collapsible open={isOpen} onOpenChange={setIsOpen}>
          <CollapsibleTrigger asChild>
            <Button variant="outline">
              <Text>Toggle</Text>
            </Button>
          </CollapsibleTrigger>
          <CollapsibleContent>
            <Text>This content can be expanded and collapsed.</Text>
          </CollapsibleContent>
        </Collapsible>
      );
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={['cli', 'manual']}>
  <Tab value="cli">
    ```bash
    npx usmds add collapsible
    ```
  </Tab>
  <Tab value="manual">
    <Steps>
      <Step>
        **Install the following dependencies:**

        ```bash
        npx expo install @rn-primitives/collapsible
        ```
      </Step>
      <Step>
        **Copy and paste the following code into your project.**

        ```json doc-gen:file
{
  "file": "../../packages/registry/src/usa/components/ui/collapsible.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"@/components/ui/collapsible.tsx\""
  }
}
        ```
      </Step>
      <Step>
        **Update the import paths to match your project setup.**
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

```tsx
import {
  Collapsible,
  CollapsibleTrigger,
  CollapsibleContent,
} from '@/components/ui/collapsible';
import { useState } from 'react';

const [isOpen, setIsOpen] = useState(false);

<Collapsible open={isOpen} onOpenChange={setIsOpen}>
  <CollapsibleTrigger asChild>
    <Button variant="outline">
      <Text>Toggle</Text>
    </Button>
  </CollapsibleTrigger>
  <CollapsibleContent>
    <Text>Content here</Text>
  </CollapsibleContent>
</Collapsible>
```

## Props

### Collapsible

Extends `@rn-primitives/collapsible` Root props.

| Prop | Type | Description |
|------|------|-------------|
| open | `boolean` | The controlled open state. |
| onOpenChange | `(open: boolean) => void` | Callback when open state changes. |
| defaultOpen | `boolean` | The default open state when initially rendered. |
| disabled | `boolean` | Whether the collapsible is disabled. |

### CollapsibleTrigger

Extends `@rn-primitives/collapsible` Trigger props.

| Prop | Type | Description |
|------|------|-------------|
| asChild | `boolean` | Render as child component (uses Slot). |

### CollapsibleContent

Extends `@rn-primitives/collapsible` Content props.

| Prop | Type | Description |
|------|------|-------------|
| forceMount | `boolean` | Force mount when more control is needed. |

