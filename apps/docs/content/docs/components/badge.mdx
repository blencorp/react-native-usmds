---
title: Badge
description: Displays a badge or a component that looks like a badge.
---

import { PreviewCard } from '@/components/ui/PreviewCard';
import { BadgePreview } from '@/components/examples';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

<Tabs items={['preview', 'code']}>
  <Tab value="preview">
    <PreviewCard preview={<BadgePreview />} />
  </Tab>
  <Tab value="code">
    ```tsx title="badge-example.tsx"
    import { Badge } from '@/components/ui/badge';
    import { Text } from '@/components/ui/text';

    export default function BadgeExample() {
      return (
        <Badge variant="default">
          <Text>Badge</Text>
        </Badge>
      );
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={['cli', 'manual']}>
  <Tab value="cli">
    ```bash
    npx usmds add badge
    ```
  </Tab>
  <Tab value="manual">
    <Steps>
      <Step>
        **Install the following dependencies:**

        ```bash
        npx expo install @rn-primitives/slot
        ```
      </Step>
      <Step>
        **Copy and paste the following code into your project.**

        ```json doc-gen:file
{
  "file": "../../packages/registry/src/usa/components/ui/badge.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"@/components/ui/badge.tsx\""
  }
}
        ```
      </Step>
      <Step>
        **Update the import paths to match your project setup.**
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

```tsx
import { Badge } from '@/components/ui/badge';
import { Text } from '@/components/ui/text';

<Badge variant="default">
  <Text>Badge</Text>
</Badge>
```

## Props

### Badge

Extends `View` props.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| variant | `'default' \| 'secondary' \| 'destructive' \| 'outline'` | `'default'` | The visual style variant. |
| asChild | `boolean` | `false` | Render as a child component (uses Slot). |
| className | `string` | - | Additional classes for styling. |

## Variants

### Default

```tsx
<Badge variant="default">
  <Text>Default</Text>
</Badge>
```

### Secondary

```tsx
<Badge variant="secondary">
  <Text>Secondary</Text>
</Badge>
```

### Destructive

```tsx
<Badge variant="destructive">
  <Text>Destructive</Text>
</Badge>
```

### Outline

```tsx
<Badge variant="outline">
  <Text>Outline</Text>
</Badge>
```

## Helper Functions

### badgeVariants

Use `badgeVariants` to create badge-styled components:

```tsx
import { badgeVariants } from '@/components/ui/badge';
import { View } from 'react-native';

<View className={badgeVariants({ variant: 'outline' })}>
  {/* content */}
</View>
```

### badgeTextVariants

Use `badgeTextVariants` for text styling within badges:

```tsx
import { badgeTextVariants } from '@/components/ui/badge';
import { Text } from '@/components/ui/text';

<Text className={badgeTextVariants({ variant: 'destructive' })}>
  Error
</Text>
```

