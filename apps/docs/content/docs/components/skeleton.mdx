---
title: Skeleton
description: Use to show a placeholder while content is loading.
---

import { PreviewCard } from '@/components/ui/PreviewCard';
import { SkeletonPreview } from '@/components/examples';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

<Tabs items={['preview', 'code']}>
  <Tab value="preview">
    <PreviewCard preview={<SkeletonPreview />} />
  </Tab>
  <Tab value="code">
    ```tsx title="skeleton-example.tsx"
    import { Skeleton } from '@/components/ui/skeleton';

    export default function SkeletonExample() {
      return (
        <div className="flex items-center gap-4">
          <Skeleton className="size-12 rounded-full" />
          <div className="flex-1 gap-2">
            <Skeleton className="h-4 w-full" />
            <Skeleton className="h-4 w-2/3" />
          </div>
        </div>
      );
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={['cli', 'manual']}>
  <Tab value="cli">
    ```bash
    npx @blen/usmds add skeleton
    ```
  </Tab>
  <Tab value="manual">
    <Steps>
      <Step>
        **Copy and paste the following code into your project.**

        ```json doc-gen:file
{
  "file": "../../packages/registry/src/usa/components/ui/skeleton.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"@/components/ui/skeleton.tsx\""
  }
}
        ```
      </Step>
      <Step>
        **Update the import paths to match your project setup.**
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

```tsx
import { Skeleton } from '@/components/ui/skeleton';

<Skeleton className="h-12 w-12 rounded-full" />
```

## Props

Extends React Native `View` props.

| Prop | Type | Description |
|------|------|-------------|
| className | `string` | Additional classes for styling. |

