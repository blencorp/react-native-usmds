---
title: Pagination
description: Pagination helps users navigate through multiple pages of content.
---

import { PreviewCard } from '@/components/ui/PreviewCard';
import { PaginationPreview } from '@/components/examples';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

<Tabs items={['preview', 'code']}>
  <Tab value="preview">
    <PreviewCard preview={<PaginationPreview />} />
  </Tab>
  <Tab value="code">
    ```tsx title="pagination-example.tsx"
    import { Pagination } from '@/components/ui/pagination';
    import { useState } from 'react';

    export default function PaginationExample() {
      const [currentPage, setCurrentPage] = useState(1);

      return (
        <Pagination
          currentPage={currentPage}
          totalPages={10}
          totalItems={100}
          onPageChange={setCurrentPage}
        />
      );
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={['cli', 'manual']}>
  <Tab value="cli">
    ```bash
    npx @blen/usmds add pagination
    ```
  </Tab>
  <Tab value="manual">
    <Steps>
      <Step>
        **Install the following dependencies:**

        ```bash
        npx expo install lucide-react-native
        ```
      </Step>
      <Step>
        **Copy and paste the following code into your project.**

        ```json doc-gen:file
{
  "file": "../../packages/registry/src/usa/components/ui/pagination.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"@/components/ui/pagination.tsx\""
  }
}
        ```
      </Step>
      <Step>
        **Update the import paths to match your project setup.**
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

```tsx
import { Pagination } from '@/components/ui/pagination';
import { useState } from 'react';

const [currentPage, setCurrentPage] = useState(1);

<Pagination
  currentPage={currentPage}
  totalPages={10}
  totalItems={100}
  onPageChange={setCurrentPage}
/>
```

## Props

### Pagination

Extends `View` props.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| currentPage | `number` | - | **Required.** The current active page number. |
| totalPages | `number` | - | **Required.** The total number of pages. |
| totalItems | `number` | - | **Required.** The total number of items across all pages. |
| onPageChange | `(page: number) => void` | - | **Required.** Callback fired when the page changes. |
| className | `string` | - | Additional classes for the container. |

